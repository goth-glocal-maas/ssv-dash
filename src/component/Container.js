import styled from "styled-components"
import { BACKGROUND_COLOR, TEXT_COLOR } from "../lib/theme"

const Container = styled.div`
  background: ${BACKGROUND_COLOR};
  color: ${TEXT_COLOR};
  height: 100vh;
`

export default Container
